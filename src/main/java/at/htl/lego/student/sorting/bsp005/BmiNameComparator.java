package at.htl.lego.student.sorting.bsp005;

import java.util.Comparator;

/**
 * Implementieren Sie einen Comparator, der Personen nach BMI und bei gleichem BMI nach Namen sortiert.
 **/
public class BmiNameComparator implements Comparator<Person> {

    @Override
    public int compare(Person p1, Person p2) {

        // TODO: Implementieren Sie diese Methode

        return Integer.MIN_VALUE;
    }



}
